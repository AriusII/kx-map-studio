<Window x:Class="KXMapStudio.Libs.Views.Dialogs.CreateFileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Create File"
        Height="280"
        Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        ResizeMode="NoResize"
        WindowStyle="None">
	<Border BorderBrush="{DynamicResource MaterialDesign.Brush.Primary}" BorderThickness="1">
		<Grid Margin="16">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0"
			           Text="Create New File"
			           Style="{StaticResource MaterialDesignHeadline5TextBlock}"
			           Margin="0,0,0,16" />

			<TextBox x:Name="FileNameTextBox"
			         Grid.Row="1"
			         materialDesign:HintAssist.Hint="File name"
			         Style="{StaticResource MaterialDesignOutlinedTextBox}"
			         Margin="0,0,0,16" />

			<TextBlock Grid.Row="2"
			           Text="File Type"
			           Style="{StaticResource MaterialDesignBody1TextBlock}"
			           Margin="0,0,0,8" />

			<StackPanel Grid.Row="3" Margin="0,0,0,16">
				<RadioButton x:Name="XmlRadioButton"
				             Content="XML"
				             IsChecked="True"
				             Margin="0,0,0,8"
				             Style="{StaticResource MaterialDesignRadioButton}" />
				<RadioButton x:Name="JsonKxv1RadioButton"
				             Content="JSON (kxv1)"
				             Margin="0,0,0,8"
				             Style="{StaticResource MaterialDesignRadioButton}" />
				<RadioButton x:Name="JsonKxv2RadioButton"
				             Content="JSON (kxv2)"
				             Style="{StaticResource MaterialDesignRadioButton}" />
			</StackPanel>

			<StackPanel Grid.Row="4"
			            Orientation="Horizontal"
			            HorizontalAlignment="Right">
				<Button Content="CANCEL"
				        Click="CancelButton_Click"
				        Style="{StaticResource MaterialDesignFlatButton}"
				        Margin="0,0,8,0" />
				<Button Content="CREATE"
				        Click="CreateButton_Click"
				        Style="{StaticResource MaterialDesignRaisedButton}" />
			</StackPanel>
		</Grid>
	</Border>
</Window>