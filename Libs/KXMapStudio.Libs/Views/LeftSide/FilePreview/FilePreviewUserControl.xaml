<UserControl x:Class="KXMapStudio.Libs.Views.LeftSide.FilePreview.FilePreviewUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/KXMapStudio.Libs;component/Themes/GlobalTheme.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Border Style="{StaticResource PanelSectionBorder}" BorderThickness="0,1,1,0">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource MaterialDesign.Brush.Surface}">
				<materialDesign:PackIcon Kind="FileEyeOutline"
				                         VerticalAlignment="Center"
				                         Margin="12,0,0,0"
				                         Foreground="{StaticResource MaterialDesign.Brush.Primary}" />
				<TextBlock Text="FILE PREVIEW" Style="{StaticResource PanelHeaderTextBlock}" />
			</StackPanel>

			<StackPanel Grid.Row="1" Margin="12,4,12,8">
				<TextBlock Text="{Binding OpenedFileName, TargetNullValue='No file selected.'}"
				           FontWeight="SemiBold"
				           FontSize="13" />
				<TextBlock Text="{Binding PreviewStatus}"
				           Opacity="0.6"
				           FontSize="11" />
				<StackPanel Orientation="Horizontal" Margin="0,4,0,0">
					<materialDesign:PackIcon Kind="ClockOutline" Width="14" Height="14" VerticalAlignment="Center" Opacity="0.6" />
					<TextBlock Text="{Binding PreviewLoadTimeText}"
					           Margin="4,0,0,0"
					           Opacity="0.6"
					           FontSize="11" />
				</StackPanel>
			</StackPanel>

			<TreeView Grid.Row="2"
			          x:Name="PreviewTreeView"
			          ItemsSource="{Binding PreviewTreeNodes}"
			          Margin="4"
			          BorderThickness="0"
			          Background="Transparent">
				<TreeView.ItemContainerStyle>
					<Style TargetType="TreeViewItem" BasedOn="{StaticResource MaterialDesignTreeViewItem}">
						<Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
						<Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
						<Setter Property="Padding" Value="6,2" />
					</Style>
				</TreeView.ItemContainerStyle>
				<i:Interaction.Triggers>
					<i:EventTrigger EventName="SelectedItemChanged">
						<i:InvokeCommandAction
							Command="{Binding SelectPreviewTreeNodeCommand}"
							CommandParameter="{Binding SelectedItem, ElementName=PreviewTreeView}" />
					</i:EventTrigger>
				</i:Interaction.Triggers>
				<TreeView.ItemTemplate>
					<HierarchicalDataTemplate ItemsSource="{Binding Children}">
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="{Binding Name}" Margin="4,2" />
							<TextBlock Text="{Binding Count, StringFormat=' ({0})'}"
							           FontWeight="SemiBold"
							           Opacity="0.6"
							           Margin="4,0,0,0">
								<TextBlock.Style>
									<Style TargetType="TextBlock">
										<Style.Triggers>
											<DataTrigger Binding="{Binding Count}" Value="0">
												<Setter Property="Visibility" Value="Collapsed" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</TextBlock.Style>
							</TextBlock>
						</StackPanel>
					</HierarchicalDataTemplate>
				</TreeView.ItemTemplate>
			</TreeView>
		</Grid>
	</Border>
</UserControl>