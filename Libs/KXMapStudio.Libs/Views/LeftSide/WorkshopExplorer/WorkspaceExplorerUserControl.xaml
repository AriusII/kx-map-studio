<UserControl x:Class="KXMapStudio.Libs.Views.LeftSide.WorkshopExplorer.WorkspaceExplorerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

	<Border Style="{StaticResource PanelSectionBorder}" BorderThickness="0,0,1,0">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<Border Grid.Row="0" Style="{StaticResource Kx.PanelHeader}">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>

					<StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
						<materialDesign:PackIcon Kind="FolderSearchOutline"
						                         Width="18" Height="18"
						                         VerticalAlignment="Center"
						                         Foreground="{StaticResource Kx.Brush.Accent}" />
						<TextBlock Text="WORKSPACE EXPLORER" Style="{StaticResource PanelHeaderTextBlock}" Margin="10,0,0,0" />
					</StackPanel>

					<Button Grid.Column="1"
					        Style="{StaticResource MaterialDesignToolButton}"
					        ToolTip="Refresh"
					        Command="{Binding RefreshCommand}"
					        VerticalAlignment="Center">
						<materialDesign:PackIcon Kind="Refresh" Width="18" Height="18" />
					</Button>
				</Grid>
			</Border>

			<TreeView Grid.Row="1"
			          ItemsSource="{Binding RootNodes}"
			          Margin="6"
			          BorderThickness="0"
			          Background="Transparent">

				<TreeView.ItemTemplate>
					<HierarchicalDataTemplate ItemsSource="{Binding Children}">
						<TextBlock Text="{Binding Name}" Margin="4,2" />
					</HierarchicalDataTemplate>
				</TreeView.ItemTemplate>

				<TreeView.ItemContainerStyle>
					<Style TargetType="TreeViewItem" BasedOn="{StaticResource MaterialDesignTreeViewItem}">
						<Setter Property="Padding" Value="6,2" />
						<Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
					</Style>
				</TreeView.ItemContainerStyle>

				<i:Interaction.Triggers>
					<i:EventTrigger EventName="SelectedItemChanged">
						<i:InvokeCommandAction Command="{Binding SelectNodeCommand}" PassEventArgsToCommand="True" />
					</i:EventTrigger>
				</i:Interaction.Triggers>

			</TreeView>
		</Grid>
	</Border>
</UserControl>