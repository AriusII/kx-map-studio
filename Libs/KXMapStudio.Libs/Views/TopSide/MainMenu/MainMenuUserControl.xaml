<UserControl x:Class="KXMapStudio.Libs.Views.TopSide.MainMenu.MainMenuUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="48" d:DesignWidth="900">

	<Border Style="{DynamicResource Kx.TopBar}">
		<DockPanel LastChildFill="True">

			<!-- Left: app actions -->
			<StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0">
				<Button Style="{StaticResource MaterialDesignToolButton}" ToolTip="Save (Ctrl+S)" Command="{Binding SaveCommand}">
					<materialDesign:PackIcon Kind="ContentSave" Width="18" Height="18" />
				</Button>
				<Button Style="{StaticResource MaterialDesignToolButton}" ToolTip="Save As (Ctrl+D)"
				        Command="{Binding SaveAsCommand}">
					<materialDesign:PackIcon Kind="ContentSaveEditOutline" Width="18" Height="18" />
				</Button>
				<Border Width="1" Height="24" Background="{StaticResource MaterialDesign.Brush.Separator}" Margin="8,0"
				        Opacity="0.6" />
				<Button Style="{StaticResource MaterialDesignToolButton}" ToolTip="Undo (Ctrl+Z)" Command="{Binding UndoCommand}">
					<materialDesign:PackIcon Kind="Undo" Width="18" Height="18" />
				</Button>
				<Button Style="{StaticResource MaterialDesignToolButton}" ToolTip="Redo (Ctrl+Y)" Command="{Binding RedoCommand}">
					<materialDesign:PackIcon Kind="Redo" Width="18" Height="18" />
				</Button>
			</StackPanel>

		</DockPanel>
	</Border>
</UserControl>