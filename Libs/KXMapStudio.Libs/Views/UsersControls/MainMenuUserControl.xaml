<UserControl x:Class="KXMapStudio.Libs.Views.UsersControls.MainMenuUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="800">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/KXMapStudio.Libs;component/Themes/GlobalTheme.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Border Background="{StaticResource MaterialDesign.Brush.Surface}" BorderThickness="0,0,0,1"
	        BorderBrush="{StaticResource MaterialDesign.Brush.Separator}">
		<Menu materialDesign:MenuAssist.TopLevelMenuItemHeight="40" Background="Transparent">
			<MenuItem Header="_File">
				<MenuItem Header="_Save" Command="{Binding SaveDocumentCommand}" InputGestureText="Ctrl+S">
					<MenuItem.Icon>
						<materialDesign:PackIcon Kind="ContentSave" />
					</MenuItem.Icon>
				</MenuItem>
				<MenuItem Header="Save _As..." Command="{Binding SaveAsCommand}">
					<MenuItem.Icon>
						<materialDesign:PackIcon Kind="ContentSaveEditOutline" />
					</MenuItem.Icon>
				</MenuItem>
				<Separator />
				<MenuItem Header="_Undo" Command="{Binding UndoCommand}" InputGestureText="Ctrl+Z">
					<MenuItem.Icon>
						<materialDesign:PackIcon Kind="Undo" />
					</MenuItem.Icon>
				</MenuItem>
				<MenuItem Header="_Redo" Command="{Binding RedoCommand}" InputGestureText="Ctrl+Y">
					<MenuItem.Icon>
						<materialDesign:PackIcon Kind="Redo" />
					</MenuItem.Icon>
				</MenuItem>
			</MenuItem>
		</Menu>
	</Border>
</UserControl>