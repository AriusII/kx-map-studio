<UserControl x:Class="KXMapStudio.Libs.Views.UsersControls.GridEditorUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:workspace="clr-namespace:KXMapStudio.Libs.ViewModels.Workspace"
             d:DataContext="{d:DesignInstance Type=workspace:GridEditorViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="pack://application:,,,/KXMapStudio.Libs;component/Themes/GlobalTheme.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</UserControl.Resources>
	<Border Style="{StaticResource PanelSectionBorder}"
	        BorderThickness="0,1,0,0"
	        HorizontalAlignment="Stretch"
	        VerticalAlignment="Stretch">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>

			<!-- Header -->
			<StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource MaterialDesign.Brush.Surface}">
				<materialDesign:PackIcon Kind="EditOutline"
				                         VerticalAlignment="Center"
				                         Margin="12,0,0,0"
				                         Foreground="{StaticResource MaterialDesign.Brush.Primary}" />
				<TextBlock Text="GRID EDITOR" Style="{StaticResource PanelHeaderTextBlock}" />
				<TextBlock Text="{Binding Status}"
				           VerticalAlignment="Center"
				           Margin="12,0,0,0"
				           Opacity="0.7"
				           FontSize="11" />
			</StackPanel>

			<!-- Content -->
			<Grid Grid.Row="1" Margin="12">
				<!-- Empty state -->
				<TextBlock Text="Sélectionnez un élément pour le modifier."
				           HorizontalAlignment="Center"
				           VerticalAlignment="Center"
				           Opacity="0.5"
				           Visibility="{Binding Rows.Count, Converter={StaticResource InverseCountToVisibilityConverter}}" />

				<!-- Data Grid -->
				<DataGrid ItemsSource="{Binding Rows}"
				          SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
				          AutoGenerateColumns="False"
				          CanUserAddRows="False"
				          CanUserDeleteRows="False"
				          CanUserReorderColumns="False"
				          CanUserSortColumns="True"
				          SelectionMode="Single"
				          IsReadOnly="False"
				          GridLinesVisibility="Horizontal"
				          HeadersVisibility="Column"
				          materialDesign:DataGridAssist.CellPadding="8"
				          materialDesign:DataGridAssist.ColumnHeaderPadding="8"
				          Visibility="{Binding Rows.Count, Converter={StaticResource CountToVisibilityConverter}}">
					<DataGrid.Columns>
						<!-- ID Column (Read-only) -->
						<DataGridTextColumn Header="ID"
						                    Binding="{Binding Id}"
						                    IsReadOnly="True"
						                    Width="60" />

						<!-- Name Column (Editable) -->
						<DataGridTextColumn Header="Name"
						                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
						                    Width="*"
						                    MinWidth="150" />

						<!-- X Column (Editable) -->
						<DataGridTextColumn Header="X"
						                    Binding="{Binding X, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"
						                    Width="100" />

						<!-- Y Column (Editable) -->
						<DataGridTextColumn Header="Y"
						                    Binding="{Binding Y, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"
						                    Width="100" />

						<!-- Z Column (Editable) -->
						<DataGridTextColumn Header="Z"
						                    Binding="{Binding Z, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"
						                    Width="100" />

						<!-- Actions Column -->
						<DataGridTemplateColumn Header="Actions" Width="Auto">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
										<Button
											Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
											CommandParameter="{Binding}"
											Style="{StaticResource MaterialDesignIconButton}"
											ToolTip="Move Up"
											Width="24"
											Height="24"
											Padding="0">
											<materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16" />
										</Button>
										<Button
											Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
											CommandParameter="{Binding}"
											Style="{StaticResource MaterialDesignIconButton}"
											ToolTip="Move Down"
											Width="24"
											Height="24"
											Padding="0"
											Margin="4,0,0,0">
											<materialDesign:PackIcon Kind="ArrowDown" Width="16" Height="16" />
										</Button>
									</StackPanel>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>
			</Grid>
		</Grid>
	</Border>
</UserControl>